package pkg;

interface Printable {

	//Abstract Method
	void print(); // Interface Method can not have any body defined i.e. Interface CAN HAVE ONLY Abstract Methods only
}// compiler internally adds 'public', 'abstract' before the interface Method and adds "public, static, final" before the data Members
